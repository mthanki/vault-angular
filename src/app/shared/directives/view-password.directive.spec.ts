import { ViewPasswordDirective } from './view-password.directive';

describe('ViewPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
