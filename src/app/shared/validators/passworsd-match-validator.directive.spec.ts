import { PassworsdMatchValidatorDirective } from './passworsd-match-validator.directive';

describe('PassworsdMatchValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PassworsdMatchValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
